---
# Live images may ship /etc/ssh/sshd_not_to_be_run; remove before enabling units.
# Strip live-only LightDM autologin drop-in; set installed default session to Steam-in-gamescope (89- drop-in).
# Ensure default host keys exist (avoids "no hostkeys available"); /run/sshd is created at boot via
# /etc/tmpfiles.d/gamebian-sshd.conf on the live/install image.
dontChroot: false
timeout: 60

script:
  - command: "rm -f /etc/ssh/sshd_not_to_be_run"
  - command: "rm -f /etc/lightdm/lightdm.conf.d/50-gamebian-live-autologin.conf"
  # Installed disk: default seat session is Steam in gamescope (overrides 88-gamebian-openbox-session.conf).
  - command: "printf '%s\\n' '[Seat:*]' 'user-session=gamebian-steam-gamescope' > /etc/lightdm/lightdm.conf.d/89-gamebian-steam-session.conf"
  - command: "ssh-keygen -A"
