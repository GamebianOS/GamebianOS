# Slim live ISO: no gamebian-web Python / flatpak on the squashfs.
# Calamares gamebian-web-install apt-installs the full runtime on the target disk
# (live session never runs gamebian-web; source is staged at /usr/src/gamebian-web).
#
# Placeholder so live-build still sees the list file (comments only = no packages).
